Simple package with single purpose of including Nette CLI laucher into Nette application with sandbox structure. Supported Nette versions are from 2.0 to 2.3@dev.

Install via `composer install rixxi/binary` and then use `vendor\bin\nette command ...`.
